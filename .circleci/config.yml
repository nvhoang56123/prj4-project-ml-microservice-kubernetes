version: 2.1
executors:
  hnguyen3-executor:
    docker:
      - image: python:3.7.3-stretch

jobs:
  build:
    executor:  hnguyen3-executor
    steps:
      - checkout
      - run: |
          # echo Hello, World!

workflows:
  my-custom-workflow:
    jobs:
      - build